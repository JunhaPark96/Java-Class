package daySixth.Game;

import java.util.*;

public class Cleric {
    String name;
    int hp = 50;
    int mp = 10;
    final int MAXHP = 50;
    final int MAXMP = 10;
    Random random = new Random();


    void selfAid() {
        this.mp -= 5;
        this.hp = MAXHP;
    }
    // 1 1~3 2 2~4 3 3~5 4 4~6
    int pray(int sec) {
        int prayedMp = sec + random.nextInt(3);
        int prayedMp = sec + random.nextInt(3);
        if (sec == 0) {
            prayedMp = 0;
        }
        if (this.mp + prayedMp > MAXMP) {
            prayedMp = MAXMP - this.mp;
            this.mp = MAXMP;
        } else {
            this.mp += prayedMp;            
        }
        return prayedMp;
    }
}
